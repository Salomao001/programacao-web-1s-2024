const aSoma = 10;
const bSoma = 15;

const aSub = 20;
const bSub = 10;

const aMulti = 5;
const bMulti = 5;

const aDiv = 25;
const bDiv = 5;

const soma = aSoma + bSoma;
const sub = aSub - bSub;
const multi = aMulti * bMulti;
const div = aDiv / bDiv;

console.log(`Resultado da soma entre ${aSoma} e ${bSoma} = ${soma}`);
console.log(`Resultado da subtração entre ${aSub} e ${bSub} = ${sub}`);
console.log(
  `Resultado da multiplicação entre ${aMulti} e ${bMulti} = ${multi}`
);
console.log(`Resultado da divisão entre ${aDiv} e ${bDiv} = ${div}`);
